# Cache — Scratch Directory Manager

The `ScratchManager` creates temporary directories for intermediate
conversion artifacts and cleans them up automatically.

::: scinterop.cache.ScratchManager
    handler: python
